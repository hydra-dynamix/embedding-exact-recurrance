Run `ldgr status` first and use `ldgr context` only when more detail is needed. Take exactly one pending work item, start one LDGR run, and complete only that bounded item. Validate the real code path and record a compact run summary containing the objective, changed files, commands, outcomes, artifacts, and next work. Avoid duplicating evidence across records. Queue follow-up work only when new scope is discovered, then close the run accurately and report the next pending item.
